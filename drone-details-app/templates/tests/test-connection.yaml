apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drone-details-app.fullname" . }}-test-connection"
  labels:
    {{- include "drone-details-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drone-details-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
